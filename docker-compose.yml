services:
  bot-party:
    build: .
    image: wecom-bot-framework:latest
    container_name: wecom-bot-party
    restart: unless-stopped
    environment:
      - BOT_NAME=party-bot
      - NODE_ENV=production
    env_file:
      - ./bots/party-bot/.env
    volumes:
      - ./bots/party-bot/config.json:/app/bots/party-bot/config.json:ro
      - ./bots/party-bot/agent.md:/app/bots/party-bot/agent.md:ro
      - ./logs:/app/logs
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  bot-test:
    build: .
    image: wecom-bot-framework:latest
    container_name: wecom-bot-test
    restart: unless-stopped
    environment:
      - BOT_NAME=pa-bot
      - NODE_ENV=production
    env_file:
      - ./bots/pa-bot/.env
    volumes:
      - ./bots/pa-bot/config.json:/app/bots/pa-bot/config.json:ro
      - ./bots/pa-bot/agent.md:/app/bots/pa-bot/agent.md:ro
      - ./logs:/app/logs
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  bot-project:
    build: .
    image: wecom-bot-framework:latest
    container_name: wecom-bot-project
    restart: unless-stopped
    environment:
      - BOT_NAME=project-bot
      - NODE_ENV=production
    env_file:
      - ./bots/project-bot/.env
    volumes:
      - ./bots/project-bot/config.json:/app/bots/project-bot/config.json:ro
      - ./bots/project-bot/agent.md:/app/bots/project-bot/agent.md:ro
      - ./logs:/app/logs
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    profiles:
      - project
